# Client Environment Variables

# API Configuration
# Leave unset for local development (uses Vite proxy to /api)
# Set for production deployment to your Cloudflare Workers URL:
# VITE_API_BASE_URL=https://bill-splitter-api.<your-subdomain>.workers.dev
