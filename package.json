{
  "name": "bill-splitter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm run dev",
    "client:build": "cd client && npm run build",
    "build": "npm run client:build && npm run size",
    "size": "size-limit",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "test": "cd server && npm test && cd ../client && npm test",
    "test:coverage": "cd server && npm run test:coverage && cd ../client && npm run test:coverage",
    "lint": "cd server && npm run lint && cd ../client && npm run lint",
    "lint:fix": "cd server && npm run lint:fix && cd ../client && npm run lint:fix",
    "format": "cd server && npm run format && cd ../client && npm run format",
    "format:check": "cd server && npm run format:check && cd ../client && npm run format:check"  },
  "devDependencies": {
    "@size-limit/preset-app": "^11.1.4",
    "@size-limit/webpack": "^11.1.4",
    "concurrently": "^8.2.2",
    "eslint": "^9.38.0",
    "prettier": "^3.6.2",
    "size-limit": "^11.1.4"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
